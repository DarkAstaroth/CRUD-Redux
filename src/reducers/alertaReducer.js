import { } from '../types';

// cada reducer tiene su state

const initialState = {
    alerta : null
}

export default function (state = initialState, action) {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}